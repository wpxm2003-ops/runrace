<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      allowBackup=false — 운동 스냅샷(정밀 GPS 경로)이 WebView localStorage에 남는데,
      자동 백업이 켜져 있으면 그 경로가 구글 백업으로 올라가고 다른 기기 복원 시 되살아난다.
      기록은 서버에 있으므로 로컬 백업으로 잃는 것은 없다.
    -->
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.runrace.app" android:host="kakao" android:pathPrefix="/callback" />
            </intent-filter>
            <!-- App Links (https://runrace.co.kr/...) — 카카오 OAuth 복귀 + 공유 링크를 앱이 직접 가로챔.
                 .well-known/assetlinks.json으로 검증. 선언한 경로만 앱이 처리(그 외는 브라우저). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="runrace.co.kr" android:pathPrefix="/kakao/callback" />
                <data android:scheme="https" android:host="runrace.co.kr" android:pathPrefix="/challenges" />
                <data android:scheme="https" android:host="runrace.co.kr" android:pathPrefix="/workouts" />
                <data android:scheme="https" android:host="runrace.co.kr" android:pathPrefix="/guides" />
                <data android:scheme="https" android:host="runrace.co.kr" android:pathPrefix="/api/share" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- 고스트/라이벌 추월 상태가 바뀔 때 짧은 햅틱 피드백을 제공한다. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 13+: the persistent foreground-location notification is part of
         the tracking contract. Declare it here instead of relying only on the
         transitive plugin manifest. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
